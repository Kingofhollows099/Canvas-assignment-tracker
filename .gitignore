# Holds your Canvas API token: never commit it.
.env
__pycache__/
*.pyc

# Assignments pushed in from the browser userscript
pushed_assignments.json
pushed_assignments.json.tmp

# Browser-sync secrets: the shared token and the self-signed TLS certificate
.sync_token
certs/
